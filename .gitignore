# build output
dist/
# generated types
.astro/

# dependencies
node_modules/
.wrangler/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production
.dev.vars

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Local checkouts of sibling repositories, dropped in here to read the SDK
# surface against. They are separate git repositories with their own history —
# ignored so they never enter this one, and excluded in tsconfig.json so
# `astro check` does not try to type-check them.
/signal-protocol-js*/
/design*/
/console*/

# Generated by scripts/demo-driver-check.mjs — a throwaway fixture build.
/.demo-driver-check/
